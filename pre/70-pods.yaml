apiVersion: v1
kind: Pod
metadata:
  labels:
    app: tiger
  name: tiger
spec:
  containers:
  - command: [ "/bin/sh", "-c"]
    args: [ "sleep infinty" ]
    image: nicolaka/netshoot
    name: main
    resources:
      requests:
        memory: "32Mi"
        cpu: "25m"
      limits:
        memory: "64Mi"
        cpu: "50m"